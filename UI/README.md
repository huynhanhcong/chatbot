# Patient Chat UI

This folder contains the standalone frontend files for the hospital patient chatbot UI.

Files:

- `index.html` - main patient chat screen
- `styles.css` - responsive patient-friendly UI styling
- `app.js` - chat behavior and `/chat` API integration

The UI expects the backend API to provide:

- `POST /chat`
